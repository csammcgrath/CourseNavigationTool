
var currentCategory = 'ACCTG';

/* ------------------------- RESET BUTTONS ------------------------- /
  Used to reset the buttons after a setting is changed.
/ -------------------------- RESET BUTTONS ------------------------ */

function reset() {
  generateButtons(currentCategory);
}
